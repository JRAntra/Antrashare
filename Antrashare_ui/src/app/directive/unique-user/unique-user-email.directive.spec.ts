import { UniqueUserEmailDirective } from './unique-user-email.directive';

describe('UniqueUserEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueUserEmailDirective();
    expect(directive).toBeTruthy();
  });
});
